// Note: keycloak.provider is intentionally NOT exported from the barrel
// to avoid loading keycloak-angular in E2E tests. Import it directly:
// import { provideKeycloakAuth } from '@orange-car-rental/auth/lib/providers/keycloak.provider';
// or import from the app.config.ts that knows when to use it.
