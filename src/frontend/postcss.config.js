// PostCSS configuration for Tailwind CSS 4
// Note: Tailwind CSS 4 requires @tailwindcss/postcss instead of tailwindcss as a plugin
module.exports = {
  plugins: [
    require('@tailwindcss/postcss'),
  ],
};
