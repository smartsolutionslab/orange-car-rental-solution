{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },
  "AllowedHosts": "*"

  // Connection strings are injected via environment variables by Aspire
  // For non-Aspire deployments, set ConnectionStrings__reservations environment variable
  // Example: ConnectionStrings__reservations=Server=sqlserver;Database=OrangeCarRental_Reservations;User Id=sa;Password=xxx
}
