{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },
  "AllowedHosts": "*",

  // Connection strings are injected via environment variables by Aspire
  // For non-Aspire deployments, set ConnectionStrings__fleet environment variable
  // Example: ConnectionStrings__fleet=Server=sqlserver;Database=OrangeCarRental_Fleet;User Id=sa;Password=xxx

  // Service URLs are resolved via Aspire Service Discovery
  // For non-Aspire deployments, set via environment variables
  "Services": {
    "Reservations": {
      "Http": {
        "0": "http://reservations-api"
      }
    }
  }
}
