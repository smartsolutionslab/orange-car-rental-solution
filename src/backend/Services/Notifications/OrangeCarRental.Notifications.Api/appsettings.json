{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },
  "AllowedHosts": "*"

  // Connection strings are injected via environment variables by Aspire
  // For non-Aspire deployments, set ConnectionStrings__notifications environment variable
  // Example: ConnectionStrings__notifications=Server=sqlserver;Database=OrangeCarRental_Notifications;User Id=sa;Password=xxx
}
